after cloning, run `git submodule update --init --recursive` to get all submodules locally

to clean all the symlink created by stow in my home folder run: `stow -D .`

to run `stow --no-folding -vt ~ nvim`
